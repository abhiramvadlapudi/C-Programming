//Check if the last digit of a number is 5 using if
#include<stdio.h>
int main()
{
    int n;
    printf("n=");
    scanf("%d",&n);
    if(n%10==5)
    {
        printf("N has last digit as 5");
    }
}